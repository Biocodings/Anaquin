#ifndef GI_FILTER_HPP
#define GI_FILTER_HPP

namespace Spike
{
    struct Filter
    {
        template <typename Options, typename F> static void filter
            (const Options &options, F f)
        {
            if (!options.filters.empty())
            {
                
            }
            else
            {
                
            }
        }
    };
}

#endif