#include <stdexcept>
#include "StructQuin/s_discover.hpp"

using namespace Anaquin;

SDiscover::Stats SDiscover::analyze(const FileName &file, const Options &o)
{
    SDiscover::Stats stats;
    
    return stats;
}

void SDiscover::report(const FileName &file, const Options &o)
{
    //const auto stats = analyze(file, o);
}